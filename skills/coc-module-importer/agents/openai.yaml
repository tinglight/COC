interface:
  display_name: "COC Module Importer"
  short_description: "Import modules and shape CoC tables"
  default_prompt: "Use $coc-module-importer to import this scenario, brief the KP, shape PL character concepts, and prepare PC-specific campaign records."
